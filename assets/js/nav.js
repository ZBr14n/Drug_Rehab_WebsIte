let selectListItem = document.querySelector('.site-nav ul')
let selectSubnav= document.querySelector('.subnav')



// if user hovers over the main nav, it should display submenu below it.
// selectListItem.addEventListener('mouseenter',(event)=>{

// })
